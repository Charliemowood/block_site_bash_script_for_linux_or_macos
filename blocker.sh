#!/bin/bash

# Remove # symbol to uncommment and run the code.
# The next four lines would allow you to block site example.com

# sudo sh -c "echo '127.0.0.1 www.example.com' >> /etc/hosts"
# sudo sh -c "echo '127.0.0.1 example.com' >> /etc/hosts"
# sudo sh -c "echo '::1 www.example.com' >> /etc/hosts"
# sudo sh -c "echo '::1 example.com' >> /etc/hosts"

# Add additional sites by copying and pasting the commented out code
# Change * for whatever site you want to block.

# sudo sh -c "echo '127.0.0.1 www.*.com' >> /etc/hosts"
# sudo sh -c "echo '127.0.0.1 *.com' >> /etc/hosts"
# sudo sh -c "echo '::1 www.*.com' >> /etc/hosts"
# sudo sh -c "echo '::1 *.com' >> /etc/hosts"
